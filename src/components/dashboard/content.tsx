import { Container, Header } from "@cloudscape-design/components"

export const Content = () => {
  return (
    <Container variant="default" header={<Header variant="h2">Your Events</Header>} />
  )
}